// 1. initialize the ability to take inputs
const prompt = require("prompt-sync")();

// 2. Ask the question / get user's input
const name = prompt("What's your name? ")

// 3. Get the length of user's name (.length)
const long = name.length

// 4. Print the results (length of name and user's name)
// console.log(`Hello ${name}, your name is ${long} characters long.`)
