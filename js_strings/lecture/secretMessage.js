// Try to return the message "run!" in the terminal with the variables given below:

let codeWord1 = "are"
let codeWord2 = "tubas"
let codeWord3 = "unsafe"
let codeWord4 = "?!"

console.log(codeWord1[1] + codeWord2[1]+ codeWord3[1] + codeWord4[1])

