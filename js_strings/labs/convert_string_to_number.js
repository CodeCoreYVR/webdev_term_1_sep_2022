const arg1 = process.argv.slice(2);


let numArr = arg1.map(str => Number(str));
numArr[0] == arg1[0] && numArr[0] !== arg1[0] ? console.log(numArr) : console.log('Please enter a number after file name!');



// [Lab] Convert String to Number
// Opens: Wednesday, 9 November 2022, 12:00 AM

// Create a script convertToNumber.js that takes in a string and converts it to a number

//   $ node ./convertToNumber.js 756

// Tip: All argument values are strings

// Tip: Google



