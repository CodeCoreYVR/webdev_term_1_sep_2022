// Find how many lists are on the homepage
document.querySelectorAll('ul') // returns NodeList(4)
document.querySelectorAll('ol') // returns NodeList(0)
// Total is 4 lists

// Find how many listed items are on the homepage
document.querySelectorAll('li')
// returns NodeList(20)

// Find how many imgs are on the homepage
document.querySelectorAll('img')
// returns NodeList(23)

// Find how many divs were used on the homepage
document.querySelectorAll('div')
// returns NodeList(137)

// Find how many stylesheets were used on the homepage
document.querySelectorAll('[rel="stylesheet"]')
// returns NodeList(6)

// Find how many paragraphs are on the homepage
document.querySelectorAll('p')
// returns NodeList(65)

// Find how many links are on the homepage
document.querySelectorAll('a')
// returns NodeList(73)

